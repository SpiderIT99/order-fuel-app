export interface IMessage {
    required: string;
    email: string;
    minlength: string;
    maxlength: string;
    min: string;
    max: string;
    phone: string;
    onlyNumbers: string;
    postalCode: string;
    buildingNumber: string;
}